{
  "name": "yamolod-event-matrix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.mjs",
    "check": "node --check server/index.mjs && node --check server/data-source.mjs",
    "probe": "node scripts/probe-data.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
